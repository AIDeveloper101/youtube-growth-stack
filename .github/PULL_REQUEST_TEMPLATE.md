## Loop

Which loop does this PR complete? (e.g. `docs/loops/002-apify-channel-scrape.md`)

## What changed

-

## Proof (required — a loop without passing proof is not done)

- [ ] `npm run build` passes
- [ ] `npm run lint` passes
- [ ] Behavioral proof from the loop's Verify section (describe or screenshot):

## Honesty checklist

- [ ] No secrets in the diff (`.env*` untouched, no keys in code/fixtures)
- [ ] Skipped or failing items are listed here explicitly, not silently omitted
- [ ] New config keys added to `.env.example` with placeholders
